      /*    // do stuff
                                        $heading_antiqua = get_sub_field('heading_antiqua');
                                        $heading_proxima = get_sub_field('heading_proxima');
                                        $copy = get_sub_field('copy');
                                        $date_or_rate = get_sub_field('date_or_rate');

                                        if ('$heading_antiqua') {
                                            echo '<h2 class="antiqua">' . $heading_antiqua . '</h2>' . $copy;
                                        }
                                        elseif ('$heading_proxima') {
                                            if ( have_rows('$date_or_rate') ) {
                                                while ( have_rows('$date_or_rate') ) : the_row();

                                                echo '<div class="solid-left">'. $date_or_rate['left_value'] .'</div>
                                                <div class="open-right">' . $date_or_rate['right_value'] . '</div>';

                                                endwhile;

                                            }
                                            endif;
                                        } */

                                                               /*

                                        // do stuff
                                        $heading_antiqua = get_sub_field('heading_antiqua');
                                        $heading_proxima = get_sub_field('heading_proxima');
                                        $copy = get_sub_field('copy');
                                        $date_or_rate = get_sub_field('date_or_rate');

                                        if ('$heading_antiqua') {
                                            echo '<h2 class="antiqua">' . $heading_antiqua . '</h2>' . $copy;
                                        }
                                        elseif ('$heading_proxima') {
                                            if ( have_rows('$date_or_rate') ) {
                                                while ( have_rows('$date_or_rate')) : the_row();
                                                echo '<div class="solid-left">'. $date_or_rate['left_value'] .'</div>
                                                <div class="open-right">' . $date_or_rate['right_value'] . '</div>';
                                                endwhile;
                                            }
                                        }
                                        endif;
                                    endwhile; */







